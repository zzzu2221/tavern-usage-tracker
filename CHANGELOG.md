# 更新日志

## v1.2.2 (2026-09-23)

### 🐛 修复
- **关闭后重启又自动打开（根本修复）**：
  - 根因：`onActivate` 调用时机太早，此时酒馆还没从磁盘加载完设置
  - 插件初始化时主动调用 `saveSettings()`，把内存里的默认值 `enabled: true` 写回了磁盘
  - 直接覆盖了你之前保存的 `enabled: false`
  - 修复：移除初始化时的主动保存，数据交给定时保存（30秒一次）和用户修改时触发

## v1.2.1 (2026-09-23)

### 🐛 修复
- **设置保存不住**：关闭「启用扩展」后重启又自动打开的问题
  - 原因：模块加载时一次性解构 `extensionSettings` 导致引用过时，修改的是旧对象
  - 修复：改为每次都从 `SillyTavern.getContext()` 获取最新上下文，和其他成熟插件保持一致
- **多余的自动初始化代码**：删除了文件底部的自动初始化块
  - 之前同时存在 hooks 调用 + 自动初始化两套机制，导致扩展被禁用后仍自动激活
  - 现在完全走 SillyTavern 标准 hooks 流程，禁用后重启不会自动打开

## v1.2.0 (2026-09-22)

### ✨ 新增
- **悬浮按钮重写**：改为圆形按钮（52px），参考微信插件实现
  - 默认位置在屏幕右侧 60% 高度，不挡移动端输入框
  - 用 pointer 事件统一处理鼠标和触摸
  - 可拖动到任意位置，位置自动保存
  - 活跃时呼吸动画效果
  - 自动修正不合理的旧位置

### 🐛 修复
- **弹窗 UI 重写**：不再依赖 SillyTavern Popup 组件，自定义全屏弹窗
  - 半透明遮罩 + 居中白色卡片
  - 内容区可垂直滚动
  - 顶部标题 + 关闭按钮，底部确定按钮
  - 移动端适配良好
- **字数统计为 0**：MESSAGE_SENT 事件传的是消息ID不是对象，修复参数解析
- **字数统计翻倍**：防重复初始化，避免事件监听器注册两次
- **移动端点击无响应**：修复 pointer button 检查逻辑，触摸事件正常响应
- **文字看不清**：全部改用固定颜色，移除主题变量依赖
- **文字阴影/重影**：全局禁用 text-shadow 和 filter
- **柱状图裁切**：收窄柱子，7根一行内显示，不要横向滚动

## v1.1.0 (2026-09-17)

### ✨ 新增
- **扩展设置面板**：在扩展管理里添加设置面板
  - 启用扩展总开关
  - 悬浮按钮显示/隐藏开关
  - 空闲超时时间可配置
  - 查看统计按钮
  - 重置今日数据按钮
- **模块自动初始化**：不依赖 hooks 机制，加载即执行
- **异步重试机制**：兼容 ST 异步构建容器

### 🐛 修复
- 首次加载弹窗不显示的问题（Popup 构造函数参数格式）

## v1.0.0 (2026-09-16)

### 🎉 首次发布
- ✅ 使用时长统计（活跃检测 + 空闲超时 + 页面可见性检测）
- ✅ PC / 移动端分开统计
- ✅ 输入字数统计
- ✅ 7 天柱状图（PC/移动端堆叠）
- ✅ 详细数据表格
- ✅ 累计统计
- ✅ 悬浮按钮（显示今日时长）
- ✅ 斜杠命令 /usage
- ✅ 数据持久化（extensionSettings）
